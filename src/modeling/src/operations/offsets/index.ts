/**
 * Polygon offsetting is a geometric operation that expands or contracts
 * shapes by a fixed distance.
 * @module modeling/offsets
 * @example
 * import { offset } from '@jscad/modeling'
 */
export { offset, OffsetOptions } from "./offset";
export { offsetFromPoints } from "./offsetFromPoints";
